import { ReflexDirective } from './reflex.directive';

describe('ReflexDirective', () => {
  it('should create an instance', () => {
    const directive = new ReflexDirective();
    expect(directive).toBeTruthy();
  });
});
